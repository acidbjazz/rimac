import styles from "@styles/plans.module.sass";

export default function Plans() {
  return (
    <section className={styles.plans}>
      <div className={styles.content}>Plans</div>
    </section>
  );
}
